{% extends 'base.html.twig' %}

{% block body %}
    <h1>Blog</h1>

    {% for post in posts %}
        <div>
            <h2>{{ post.title }}</h2>
            <p>{{ post.lead }}</p>
        </div>
    {% endfor %}
{% endblock %}

{% block stylesheets %}
{% endblock %}
